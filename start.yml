---
- name: Running Docker
  hosts: all
  become: true
  gather_facts: false
  tasks:
  
    - name: pull an image
      shell: |
        docker run -it --name jmeter -w /jmeter/bin/project -v ${PWD}:/jmeter/bin/project harishpallapu/jmeter-3i:latest
